name: flutter_application_1
description: "A new Flutter project."

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1  # Asegúrate de actualizar la versión según sea necesario para el despliegue

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.0.13
  provider: ^6.0.0
  http: ^0.13.6
  flutter_secure_storage: ^5.0.0+1
  cupertino_icons: ^1.0.8
  intl: ^0.19.0  # Dependencia para la internacionalización
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/lang/en.json  # Traducciones en inglés
    - assets/lang/es.json  # Traducciones en español
  uses-material-design: true

